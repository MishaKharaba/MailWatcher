package ExchangeActiveSync;

public class ASError {
	// This method simply displays the exception to the user.
	public static void reportException(Exception ex) {
		// Log.i(ex.Message);
		// Console.WriteLine(ex.TargetSite.ToString());
	}
}
